<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="account_journal_form_inherit" model="ir.ui.view">
        <field name="name">account.journal.form.inherit</field>
        <field name="model">account.journal</field>
        <field name="inherit_id" ref="account.view_account_journal_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='country_code']" position='after'>
                <field name="apply_voucher" invisible="1"/>
                <field name="is_vale" invisible="not apply_voucher"/>
                <field name="partner"
                    invisible="not is_vale or not apply_voucher"
                    required="is_vale"
                />
            </xpath>
        </field>
    </record>
</odoo>
